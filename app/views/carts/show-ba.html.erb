<h2>Your Pragmatic Cart</h2>

<ul>
	<% @cart.line_items.each do |item| %>
		<li><%= item.quantity %> &times; <%= item.product.title %></li>
	<% end %>
</ul>

<%= button_to 'Empth cart', @cart, :method => :delete, :confirm => 'Are you sure?' %>